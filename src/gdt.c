#include "gdt.h"

// internal gdt structure representing the actual bit layout of a gdt entry in hardware
struct _gdt_entry {
};

// internal gdt table representing the actual bit layout of the gdt in hardware
struct _gdt_table {
};

void gdt_init() {}
void gdt_reinit() {}

